export interface Post {
  id: string;
  title: string;
  content: string;
  imagePath: string;
  // publisher?: string;
  // updated_date?: { type: Date, default: Date.now },
  // voteAmount: number;
}
